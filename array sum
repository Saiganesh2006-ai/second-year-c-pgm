#include<stdio.h>
int Arraysum(int arr[], int n){
    if(n == 0){
        return arr[n-1];
    }
    return arr[n-1] + Arraysum(arr,n-1);
}
void main(){
    int n,i=0;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i=n;i++){
        scanf("%d",&arr[i]);
    }
    Arraysum(arr,n);
}
